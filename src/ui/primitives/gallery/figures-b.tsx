/*
 * Gallery figures for the primitives owned by agent B (rows, overlays, feedback,
 * keys). Register stage contents with register("<Sheet>", { "<caption>": () => … }).
 * See registry.ts for keys and forced states (data-force).
 */
export {};
