# Android presentation replacement

Status: clean Android rebuild delivered to the authorized TV; remaining per-screen visual qualification is tracked in the linked issues. The owner rejected the prior Android presentation on 2026-09-12. Previous build and behavior checks do not constitute visual acceptance. Android implementation issue: viptv-org/android#3; design tracking: #6.

Replace the entire Android Compose presentation with the current frozen Roku screens. Keep the existing authenticated account, metadata and playback interfaces, device pairing and direct-first Media3 adapter. Do not add pages or actions that the Roku implementation does not expose. The new presentation uses the same packaged artwork, canonical 1280×720 coordinates, typography hierarchy, focus fills, card clipping, remote input meanings and screen transitions.

Actual dynamic Roku layout overrides take precedence over a mistaken extraction. Correct the corresponding design text when a discrepancy is found. In particular Home is a fixed background and independently clipped shelf viewport, never a vertically scrolling page containing the hero. The current implementation hides the hero for lower shelves. Media decoding must retain backdrop, thumbnail, year, runtime, genre and credits fields so screens can render the source content.

Implement all screens before the integrated hosted build and device acceptance. No per-edit test/build loop and no local Gradle or emulators. Final review compares matching Roku and Android screens at the same content, state and resolution, inspecting layout regions as well as the whole composition. The owner's target is at least 90% visual similarity; do not report that target achieved without a reproducible comparison. Screenshots and comparison images remain private, never checked in. Record measured results and remaining differences in text.

The clean implementation replaces Home, Discover, collections, movie and series details, sources, Guide, Search, Settings, pairing, profiles, avatar selection, parent prompts and player chrome. It keeps Media3 behind the existing playback interface. Review corrections include exact lower-shelf placement, artwork-only card focus, metadata artwork propagation, episode progress targeting and modal/rail focus restoration. No screenshots are stored here. Final measured acceptance belongs in Android TESTING.md; a comparison on one Home state never certifies every screen.

Delivery evidence: Android `0818892b235e7c1698e6ba591c7b7527ca619405`, hosted review 34725540311 (84 passing tests), installed update with pairing retained. Private matched-state SSIM reached 0.9301 Home hero, 0.9471 Home shelves, 0.9485 Settings and 0.9100 Search. Episode page measured 0.8723; uniform 90% across every screen/state is not certified. See [Android TESTING.md](https://github.com/viptv-org/android/blob/main/TESTING.md) for physical playback/navigation checks, cleanup and remaining differences.
